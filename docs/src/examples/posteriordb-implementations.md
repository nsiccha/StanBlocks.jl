# Julia posteriordb implementations

See the `PosteriorDB.jl` extension at
[`ext/PosteriorDBExt.jl`](https://github.com/nsiccha/StanBlocks.jl/blob/devibe/ext/PosteriorDBExt.jl).

The Quarto page embedded that file directly. The extension is the maintained
source, so the VitePress page links to it rather than copying a second version.
```
